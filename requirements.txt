# Local (laptop) requirements. Python 3.11+.
# The GPU-only packages (chatterbox-tts, torch) are installed *inside the
# Kaggle notebook*, never here -- see kaggle/make_voice_nb.py.

pillow>=10.2          # all chart/card drawing
requests>=2.31        # Wikimedia Commons photo fetch
kaggle>=1.6           # notebook push / status / output  (2.2.x also supported)

# Optional but recommended: local narration, free and offline. For Shorts this
# is the PRODUCTION voice, not just a draft -- it needs no GPU and no Kaggle job.
#   pip install kokoro-onnx soundfile
# Model files: take kokoro-v1.0.onnx and voices-v1.0.bin from the kokoro-onnx
# GitHub releases page and put them in the project root. GitHub releases, not
# Hugging Face -- this project does not depend on the Hub.
# Without it, `build_body.py` falls back to --voice silent.
